import styled from 'styled-components';

const Wrapper = styled.div`
	display: flex;
	align-items: center;
	flex-flow: column nowrap;
	width: 100%;
	max-width: 600px;
	margin: auto;
	height: 100vh;
`;

export default {
	Wrapper
};
