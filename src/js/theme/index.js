import typography from './typography';
import colors from './colors';
import setTransition from './transitions';
import mediaQuery from './mediaQuery';

const theme = {
	typography,
	colors,
	mediaQuery,
	setTransition
};

export default theme;

export { typography, colors, mediaQuery, setTransition };
